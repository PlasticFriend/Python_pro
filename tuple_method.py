# tuple can be converted to a list andmodified and then changed again to tuple
# can add tuples
tup1 = (1,2,3,4,5)
tup2 = (7,8,9,10)
tup = tup1+tup2
print(tup1.count(3))
print(tup1.index(3))
print(tup1.index(3,1,4)) # will find index of 3 between the index 1 and 4
# can also use len method
# trying pychrm push